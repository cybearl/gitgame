/**
 * The configuration options for the status bar task field.
 */

const STATUS_CONFIG = {
    indeterminateTickMs: 150,
    indeterminateTickStep: 5,
    disabledTask: {
        id: "disabled",
        label: "N/A",
    },
}

export default STATUS_CONFIG

/**
 * The general config for the CLI application.
 */
const GENERAL_CONFIG = {
    name: "gitgame",
    version: "0.1.0",
    description: "A small CLI tool to simplify UE5-based game development with Git LFS.",
    config: {
        dirname: "gitgame",
        filename: "config.json",
    },
}

export default GENERAL_CONFIG
